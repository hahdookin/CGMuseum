#tool
extends MeshInstance

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
    pass
#    get_active_material(0).set_shader_param("center", Vector2(global_transform.origin.x, global_transform.origin.z))
#    get_active_material(0).set_shader_param("size", mesh.size)
